# PROJECT_OVERVIEW

## 1. מטרת הפרויקט
יישום מקומי לניהול תקציב משפחתי עם דשבורד חודשי/שנתי, עדכון מהיר של סכומים, ותמיכה בהרחבה עתידית של תכונות ניהול.

## 2. מה יש כרגע (Implemented)
### Frontend
- דף חודשי: הצגת קטגוריות, סעיפי תקציב, סרגלי התקדמות וסכומי סיכום.
- דף שנתי: תרשים לפי חודשים וקטגוריות עם акорדיון להצגה מרוכזת.
- מודל “דיווח מהיר”: כפתור צף, overlay, חיפוש, בחירה ושליחה של סכום ל-API.
- דף “ניהול והגדרות”: קיים כרגע כעמוד placeholder בלבד.

### Backend
- שרת Express שמגיש קבצי סטטיק ומספק REST API.
- נקודות קצה עיקריות:
  - GET /api/month/:month — שליפת רשומות חודשיות או יצירה אוטומטית אם חסרות.
  - POST /api/update-actual — עדכון actual_amount של רשומה בודדת.
  - GET /api/annual-summary — סיכום שנתי לפי חודשים וקטגוריות.

### Database
- SQLite קובץ-מבוסס (budget.db), עם אתחול מ-setup.sql בטעינת השרת.
- טבלאות מרכזיות:
  - categories: קטגוריות תקציב עם type INCOME/EXPENSE.
  - budget_items: סעיפי תקציב עם default_planned ו-foreign key ל-categories.
  - monthly_records: רשומות חודשיות לפי month (YYYY-MM), עם planned_amount ו-actual_amount.
  - holiday_fund: קופת חגים עם תיאור וסכום.

## 3. מה יהיה בהמשך (Planned)
- הרחבת דף ההגדרות לניהול קטגוריות, סעיפים, ותצורות מערכת.
- הוספת אפשרויות עריכה/הוספה/מחיקה של סעיפים ורשומות.
- שיפור מנגנון ה-API וה-DB לכיוון ארכיטקטורה יותר מודולרית (routes/services/validation).
- תוספת תכונות ניתוח נוספות: פילטרים, גרפים נוספים, השוואות בין חודשים ושנים.
- שיפור UX: חיפוש מתקדם, שמירה אוטומטית, יצוא/יבוא נתונים, וקופות/קטגוריות נוספות.

## 4. עקרונות פיתוח קבועים
- Frontend: HTML/CSS/JavaScript טהור, ללא framework בשלב זה.
- Backend: Express + SQLite, עם תקשורת JSON.
- ממשק RTL עם שמירה על direction: ltr עבור נתונים מספריים במקומות קריטיים.
- שימוש קבוע בשלושת צבעי הסרגלים: bg-normal, bg-warning, bg-danger.
- הניווט מתבצע דרך switchPage(), וכל דף חדש אמור להופיע כ-element עם class .page/.active.