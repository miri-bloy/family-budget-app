# AGENT_STEPS & BEHAVIOR PROTOCOL

זהו פרוטוקול העבודה המחייב מול המשתמשת במצב סוכן (Agent Mode). עליך לפעול לפיו כדי להבטיח עבודה עצמאית, תיקון ישיר של הקבצים, ודיווח תמציתי וממוקד.

---

## ⚡ זרימת עבודה ואוטונומיה מלאה (Autonomous Execution)

כאשר המשתמשת מבקשת משימה, תיקון או תוספת, עליך לבצע את השלבים הבאים בדיאלוג איתה:

### שלב 1: סיכום קצר של המשימה (לפני הביצוע)
* לפני שאתה נוגע בקבצים, כתוב למשתמשת סיכום קצר וברור (1-2 משפטים) של המשימה כפי שהבנת אותה, וציין אילו קבצים אתה הולך לערוך.

### שלב 2: תיקון הקבצים בפועל (Direct File Editing)
* גש ישירות לקבצי הפרויקט הרלוונטיים ותקן/עדכן אותם בעצמך על המקום.
* בצע את השינויים על פי ההחלטה המקצועית שלך, תוך התחשבות מלאה בארכיטקטורה ובחוקי ה-RTL המוגדרים בקובץ `PROJECT_OVERVIEW.md`.
* **אל תציג קטעי קוד ארוכים להדבקה בצ'אט** – המטרה היא שאתה תבצע את העריכה בקבצים עצמם.

### שלב 3: סיכום קצר של השינויים בפועל (לאחר הביצוע)
* לאחר שסיימת לערוך את הקבצים, ספק סיכום קצר, קולע וטכני בנקודות שמסביר למשתמשת בדיוק מה שינית ומה השתנה בפועל (אילו פונקציות, קלאסים או אלמנטים הוספת או עדכנת בכל קובץ).
* אין צורך להריץ את המערכת בסיום השינוי, המשתמשת תבצע זאת בעצמה ותבדוק את התוצאה

---

## 🛠️ חוקי ברז קבועים בעריכת הקבצים

1. **שמירה על חוקי ה-RTL והצגת מספרים:** כל קוד HTML/CSS חדש שאתה כותב חייב לתמוך בכיווניות האתר. זכור שמחרוזות המציגות יחסי מספרים (כמו `X / Y`) נוטות להתהפך ב-RTL. לכן, עטוף אותן תמיד באלמנט המגדיר כיווניות הפוכה נקודתית בתוך ה-HTML: `style="direction: ltr; display: inline-block;"`.
2. **שימוש ב-Theme הקיים:** השתמש אך ורק בשלישיית הקלאסים הקיימת לצבעי סרגלים וסטטוסים: `bg-normal` (ירוק), `bg-warning` (צהוב), `bg-danger` (אדום). אל תמציא קלאסים חדשים לצבעים.